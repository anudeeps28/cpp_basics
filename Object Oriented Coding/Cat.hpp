#pragma once

// this is the header file, which contains prototpye (just the prototype, not the implementation. The implementation is present in cpp source file) of the function present in the cpp file

class Cat{ // defining the class
public: // making functions publis = making these available outside where this class is not associated also
    void speak();
    void jump();
};


